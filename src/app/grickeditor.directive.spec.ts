import { GrickeditorDirective } from './grickeditor.directive';

describe('GrickeditorDirective', () => {
  it('should create an instance', () => {
    const directive = new GrickeditorDirective();
    expect(directive).toBeTruthy();
  });
});
